package org.munta.algorithm;

import org.munta.model.EntityCollection;
import org.munta.model.Regularity;

public class ProbabilityAlgorithm {
    public boolean checkRegularity(Regularity regularity, EntityCollection entities) {
        return false;
    }
}
